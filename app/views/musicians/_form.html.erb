<div class="container form-container bg-white p-4 border">
  <%= simple_form_for([@festival, @musician]) do |f| %>
    <%= f.input :name %>
    <%= f.input :instrument %>
     <%= f.association :requirements, as: :check_boxes %>
    <%= f.submit %>
  <% end %>
</div>
