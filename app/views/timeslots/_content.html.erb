<div class="header p-3">
    <div class="header-content d-flex flex-column">
      <div class="row">
        <div class="col-6" >
            <i class="fa-solid fa-hourglass-start pe-2"></i></i><span>Start Time</span>
          </div>
          <div class="col-6">
            <i class="fa-solid fa-hourglass-end pe-2"></i></i><span>End Time</span>
          </div>
        </div>
    </div>
  </div>

<% timeslots.each do |timeslot| %>

  <%= render "timeslot_row", timeslot: timeslot %>

<% end %>
